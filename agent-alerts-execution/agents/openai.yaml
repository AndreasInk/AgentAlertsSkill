interface:
  display_name: "Agent Alerts Execution"
  short_description: "Send trusted webhook alerts to iPhone"
  default_prompt: "Use $agent-alerts-execution to send or skip an Agent Alerts update through the hosted HTTPS webhook with the bundled script."
