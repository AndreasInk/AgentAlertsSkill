interface:
  display_name: "Agent Alerts Execution"
  short_description: "Choose and send trusted iPhone alert variants"
  default_prompt: "Use $agent-alerts-execution to choose an appropriate Live Activity presentation, then send or skip the Agent Alerts update through the reviewed hosted webhook."
